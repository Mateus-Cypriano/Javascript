// insere caracteres antes da string.
console.clear();

let sku = "34";

console.log(sku.padStart(6, "0")); // No metódo primeiro passamos a quantidade que caracteres a string deve conter, e depois o que vai completar a string. 

let sku2 = "1883";

console.log(sku2.padStart(6, "0"));
