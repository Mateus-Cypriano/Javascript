console.clear()
const numeros = [1, 2, 3, 4, 5, 6];

//metodo forEach funciona como um laço de repetição dentro de uma array. 'Para cada elemento do objeto(array)'
numeros.forEach(num => {
    console.log(num);
});

let nomes = ["Mateus", "Maria", "José", "Pedro", "João"];

nomes.forEach(nome => {
    console.log("O nome é " + nome);
})

